# 闲时检测器优化方案

## 修正后的闲时检测逻辑

### 原方案的问题
- ❌ 依赖用户空闲时间（用户可能在看视频、阅读文档）
- ❌ 5 分钟太长，错过很多闲时窗口
- ❌ 没有执行频率限制

### 新方案的优势
- ✅ 只看系统资源状态（CPU/内存/充电）
- ✅ 1 分钟即可触发（更灵活）
- ✅ 1 小时最多执行 1 次（可配置）

---

## 一、闲时检测逻辑

### 检测条件（全部满足）

```python
1. CPU 使用率 < 20%（持续 1 分钟）
2. 内存使用率 < 70%（持续 1 分钟）
3. 笔记本需在充电
4. 距离上次执行 > 1 小时（可配置）
```

### 执行流程

```
每 10 秒检查一次
  ↓
检查执行间隔（1 小时内已执行？）
  ↓ No
检查资源状态（CPU < 20% && 内存 < 70% && 充电？）
  ↓ Yes
开始计时（资源状态稳定）
  ↓
持续 1 分钟？
  ↓ Yes
触发闲时任务
  ↓
标记执行时间
  ↓
1 小时后才能再次执行
```

---

## 二、核心代码实现

### IdleDetectorV2 类

```python
"""
闲时检测器 v2.0

检测逻辑：
1. CPU 使用率 < 20%（持续 1 分钟）
2. 内存使用率 < 70%（持续 1 分钟）
3. 笔记本需在充电
4. 距离上次执行 > 1 小时（可配置）
"""

import time
import psutil
import logging
from datetime import datetime, timedelta
from typing import Optional

logger = logging.getLogger(__name__)


class IdleDetectorV2:
    """系统资源闲时检测器（不依赖用户行为）"""

    def __init__(
        self,
        cpu_threshold: float = 20.0,      # CPU 阈值 20%
        memory_threshold: float = 70.0,   # 内存阈值 70%
        stable_duration: int = 60,        # 稳定持续时间 60 秒
        min_interval: int = 3600,         # 最小执行间隔 1 小时
        require_power: bool = True,       # 是否要求充电
    ):
        self.cpu_threshold = cpu_threshold
        self.memory_threshold = memory_threshold
        self.stable_duration = stable_duration
        self.min_interval = min_interval
        self.require_power = require_power

        # 状态追踪
        self._last_execution_time: Optional[datetime] = None
        self._stable_start_time: Optional[float] = None

    # ── 公共接口 ──────────────────────────────────────────────────────

    def is_idle(self) -> bool:
        """
        判断系统是否处于闲时。

        Returns:
            True: 可以执行闲时任务
            False: 不满足条件
        """
        # 1. 检查执行间隔（1 小时内已执行过）
        if not self._check_execution_interval():
            return False

        # 2. 检查系统资源状态
        if not self._check_resource_state():
            # 资源不满足，重置稳定计时器
            self._reset_stable_timer()
            return False

        # 3. 检查稳定持续时间（1 分钟）
        if not self._check_stable_duration():
            return False

        logger.info("系统进入闲时状态，可执行闲时任务")
        return True

    def mark_execution(self) -> None:
        """标记闲时任务已执行（更新最后执行时间）"""
        self._last_execution_time = datetime.now()
        self._reset_stable_timer()
        logger.info(
            "闲时任务执行完成，下次执行时间: %s",
            self._last_execution_time + timedelta(seconds=self.min_interval)
        )

    def get_time_until_next_execution(self) -> int:
        """获取距离下次执行的秒数"""
        if self._last_execution_time is None:
            return 0  # 从未执行过，立即可执行

        next_time = self._last_execution_time + timedelta(seconds=self.min_interval)
        delta = (next_time - datetime.now()).total_seconds()
        return max(0, int(delta))

    # ── 内部检查方法 ──────────────────────────────────────────────────

    def _check_execution_interval(self) -> bool:
        """检查距离上次执行是否超过最小间隔"""
        if self._last_execution_time is None:
            return True  # 从未执行过

        elapsed = (datetime.now() - self._last_execution_time).total_seconds()
        if elapsed < self.min_interval:
            logger.debug(
                "距离上次执行仅 %.0f 秒，需等待 %.0f 秒",
                elapsed,
                self.min_interval - elapsed
            )
            return False

        return True

    def _check_resource_state(self) -> bool:
        """检查系统资源状态（CPU + 内存 + 充电）"""
        # 1. 检查 CPU 使用率
        cpu_usage = psutil.cpu_percent(interval=1)
        if cpu_usage > self.cpu_threshold:
            logger.debug("CPU 使用率过高: %.1f%% > %.1f%%",
                        cpu_usage, self.cpu_threshold)
            return False

        # 2. 检查内存使用率
        memory = psutil.virtual_memory()
        if memory.percent > self.memory_threshold:
            logger.debug("内存使用率过高: %.1f%% > %.1f%%",
                        memory.percent, self.memory_threshold)
            return False

        # 3. 检查是否在充电（笔记本）
        if self.require_power and not self._is_on_power():
            logger.debug("笔记本未充电，跳过闲时任务")
            return False

        logger.debug(
            "资源状态良好: CPU %.1f%%, 内存 %.1f%%",
            cpu_usage,
            memory.percent
        )
        return True

    def _check_stable_duration(self) -> bool:
        """检查资源状态是否稳定持续 1 分钟"""
        current_time = time.monotonic()

        # 首次进入稳定状态
        if self._stable_start_time is None:
            self._stable_start_time = current_time
            logger.debug("资源状态开始稳定，计时开始")
            return False

        # 计算已稳定时长
        stable_elapsed = current_time - self._stable_start_time

        if stable_elapsed < self.stable_duration:
            logger.debug(
                "资源状态稳定中: %.0f/%.0f 秒",
                stable_elapsed,
                self.stable_duration
            )
            return False

        # 已稳定超过 1 分钟
        logger.info("资源状态已稳定 %.0f 秒，满足闲时条件", stable_elapsed)
        return True

    def _reset_stable_timer(self) -> None:
        """重置稳定计时器"""
        self._stable_start_time = None

    def _is_on_power(self) -> bool:
        """检查是否在充电（笔记本）"""
        try:
            battery = psutil.sensors_battery()
            if battery is None:
                # 台式机，无电池
                return True
            return battery.power_plugged
        except Exception as e:
            logger.warning("无法检测充电状态: %s", e)
            return True  # 检测失败，默认允许执行

    # ── 状态查询 ──────────────────────────────────────────────────────

    def get_status(self) -> dict:
        """获取当前状态（用于调试和监控）"""
        cpu_usage = psutil.cpu_percent(interval=0)
        memory = psutil.virtual_memory()
        battery = psutil.sensors_battery()

        stable_elapsed = 0
        if self._stable_start_time is not None:
            stable_elapsed = time.monotonic() - self._stable_start_time

        return {
            "cpu_usage": cpu_usage,
            "cpu_ok": cpu_usage < self.cpu_threshold,
            "memory_usage": memory.percent,
            "memory_ok": memory.percent < self.memory_threshold,
            "on_power": battery.power_plugged if battery else True,
            "stable_elapsed": int(stable_elapsed),
            "stable_required": self.stable_duration,
            "time_until_next": self.get_time_until_next_execution(),
            "can_execute": self.is_idle(),
        }
```

---

## 三、使用示例

### 基本使用

```python
import asyncio
from idle_detector_v2 import IdleDetectorV2

async def main():
    # 创建检测器
    detector = IdleDetectorV2(
        cpu_threshold=20.0,      # CPU < 20%
        memory_threshold=70.0,   # 内存 < 70%
        stable_duration=60,      # 稳定 1 分钟
        min_interval=3600,       # 1 小时执行 1 次
        require_power=True,      # 需要充电
    )

    # 每 10 秒检查一次
    while True:
        if detector.is_idle():
            print("✅ 系统闲时，开始执行任务")

            # 执行闲时任务
            await execute_idle_tasks()

            # 标记执行完成
            detector.mark_execution()

            print(f"⏰ 下次执行时间: {detector.get_time_until_next_execution()} 秒后")

        else:
            # 打印当前状态
            status = detector.get_status()
            print(f"⏳ 等待闲时: CPU {status['cpu_usage']:.1f}%, "
                  f"内存 {status['memory_usage']:.1f}%, "
                  f"稳定 {status['stable_elapsed']}/{status['stable_required']} 秒")

        await asyncio.sleep(10)

async def execute_idle_tasks():
    """执行闲时任务"""
    print("开始批量处理...")
    await asyncio.sleep(5)  # 模拟任务执行
    print("任务完成")

if __name__ == "__main__":
    asyncio.run(main())
```

---

### 输出示例

```
⏳ 等待闲时: CPU 45.2%, 内存 65.3%, 稳定 0/60 秒
⏳ 等待闲时: CPU 18.5%, 内存 68.1%, 稳定 10/60 秒
⏳ 等待闲时: CPU 15.3%, 内存 66.7%, 稳定 20/60 秒
⏳ 等待闲时: CPU 12.8%, 内存 65.2%, 稳定 30/60 秒
⏳ 等待闲时: CPU 10.1%, 内存 64.8%, 稳定 40/60 秒
⏳ 等待闲时: CPU 8.5%, 内存 63.9%, 稳定 50/60 秒
✅ 系统闲时，开始执行任务
开始批量处理...
任务完成
⏰ 下次执行时间: 3600 秒后
```

---

## 四、配置文件

### config.yaml

```yaml
idle_detector:
  # CPU 阈值（百分比）
  cpu_threshold: 20.0

  # 内存阈值（百分比）
  memory_threshold: 70.0

  # 资源状态稳定持续时间（秒）
  stable_duration: 60

  # 最小执行间隔（秒）
  # 3600 = 1 小时
  # 7200 = 2 小时
  # 1800 = 30 分钟
  min_interval: 3600

  # 是否要求充电（笔记本）
  require_power: true

  # 检查间隔（秒）
  check_interval: 10
```

---

## 五、与任务调度器集成

### 完整示例

```python
import asyncio
from idle_detector_v2 import IdleDetectorV2
from task_scheduler import TaskScheduler
from model_manager import ModelManager

async def idle_worker():
    """闲时任务工作线程"""
    detector = IdleDetectorV2(
        cpu_threshold=20.0,
        memory_threshold=70.0,
        stable_duration=60,
        min_interval=3600,
    )

    scheduler = TaskScheduler()
    model_manager = ModelManager()

    while True:
        # 每 10 秒检查一次
        await asyncio.sleep(10)

        if not detector.is_idle():
            continue

        # 进入闲时，开始处理任务
        logger.info("🌙 进入闲时模式，开始批量处理")

        try:
            # 1. 处理 Embedding 任务
            embedding_tasks = scheduler.get_pending_tasks("embedding")
            if embedding_tasks:
                logger.info("📊 加载 BGE-M3 模型...")
                model = model_manager.load("bge-m3-int8")

                logger.info("🔄 批量处理 %d 条 Embedding 任务", len(embedding_tasks))
                await process_embedding_batch(model, embedding_tasks)

                logger.info("🗑️ 卸载 BGE-M3 模型")
                model_manager.unload("bge-m3-int8")

            # 2. 处理 VLM 任务
            vlm_tasks = scheduler.get_pending_tasks("vlm")
            if vlm_tasks:
                logger.info("👁️ 加载 MiniCPM-V 模型...")
                model = model_manager.load("minicpm-v-q2")

                logger.info("🔄 批量处理 %d 条 VLM 任务", len(vlm_tasks))
                await process_vlm_batch(model, vlm_tasks)

                logger.info("🗑️ 卸载 MiniCPM-V 模型")
                model_manager.unload("minicpm-v-q2")

            # 3. 处理 ASR 任务
            asr_tasks = scheduler.get_pending_tasks("asr")
            if asr_tasks:
                logger.info("🎤 加载 Whisper 模型...")
                model = model_manager.load("whisper-tiny-q5")

                logger.info("🔄 批量处理 %d 条 ASR 任务", len(asr_tasks))
                await process_asr_batch(model, asr_tasks)

                logger.info("🗑️ 卸载 Whisper 模型")
                model_manager.unload("whisper-tiny-q5")

            logger.info("✅ 闲时任务全部完成")

        except Exception as e:
            logger.error("闲时任务执行失败: %s", e)

        finally:
            # 标记执行完成
            detector.mark_execution()
            logger.info("⏰ 下次闲时任务: %d 秒后",
                       detector.get_time_until_next_execution())
```

---

## 六、优势对比

### 原方案 vs 新方案

| 对比项 | 原方案 | 新方案 |
|--------|--------|--------|
| 检测依据 | 用户空闲时间 | 系统资源状态 |
| 触发条件 | 5 分钟无操作 | CPU/内存稳定 1 分钟 |
| 执行频率 | 无限制 | 1 小时最多 1 次 |
| 误触发 | 用户看视频时触发 | 不会误触发 |
| 灵活性 | 低 | 高（可配置） |
| 用户体验 | 可能打断用户 | 完全无感知 |

---

## 七、测试用例

### 单元测试

```python
import pytest
from idle_detector_v2 import IdleDetectorV2
from datetime import datetime, timedelta

def test_first_execution_allowed():
    """首次执行应该立即允许"""
    detector = IdleDetectorV2(min_interval=3600)
    assert detector.get_time_until_next_execution() == 0

def test_execution_interval_enforced():
    """执行间隔应该被强制执行"""
    detector = IdleDetectorV2(min_interval=3600)

    # 标记执行
    detector.mark_execution()

    # 立即检查，应该不允许
    assert detector._check_execution_interval() == False

    # 模拟 1 小时后
    detector._last_execution_time = datetime.now() - timedelta(seconds=3601)
    assert detector._check_execution_interval() == True

def test_stable_duration_tracking():
    """稳定持续时间应该被正确追踪"""
    detector = IdleDetectorV2(stable_duration=60)

    # 首次检查，开始计时
    assert detector._check_stable_duration() == False
    assert detector._stable_start_time is not None

    # 模拟 30 秒后
    import time
    detector._stable_start_time = time.monotonic() - 30
    assert detector._check_stable_duration() == False

    # 模拟 61 秒后
    detector._stable_start_time = time.monotonic() - 61
    assert detector._check_stable_duration() == True
```

---

## 八、监控和调试

### 状态监控 API

```python
from fastapi import FastAPI

app = FastAPI()
detector = IdleDetectorV2()

@app.get("/idle/status")
def get_idle_status():
    """获取闲时检测器状态"""
    return detector.get_status()

@app.get("/idle/next_execution")
def get_next_execution():
    """获取下次执行时间"""
    return {
        "seconds_until_next": detector.get_time_until_next_execution(),
        "can_execute_now": detector.is_idle(),
    }
```

### 响应示例

```json
{
  "cpu_usage": 15.3,
  "cpu_ok": true,
  "memory_usage": 65.2,
  "memory_ok": true,
  "on_power": true,
  "stable_elapsed": 45,
  "stable_required": 60,
  "time_until_next": 2847,
  "can_execute": false
}
```

---

## 总结

### 核心改进

1. ✅ **不依赖用户行为** - 只看系统资源状态
2. ✅ **更灵活的触发** - 1 分钟稳定即可触发
3. ✅ **频率限制** - 1 小时最多执行 1 次
4. ✅ **可配置** - 所有参数都可调整
5. ✅ **状态追踪** - 完整的状态查询 API

### 预期效果

- 每小时自动找到 1 次闲时窗口
- 批量处理积压任务
- 用户完全无感知
- 避免内存爆炸

需要我开始实施这个优化方案吗？